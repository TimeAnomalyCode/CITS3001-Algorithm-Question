INT(1, 1000000, n) SPACE INT(1, 1000000, r) SPACE INT(1, 1000000, d) NEWLINE

REP(d, SPACE)
    REGEX("[A-Z]+")
END
NEWLINE

INT(0, 0) SPACE REGEX("[A-Z]") NEWLINE

REP(r)
    INT(0, n) SPACE INT(1, n) SPACE REGEX("[A-Z+.]") NEWLINE
END
EOF
